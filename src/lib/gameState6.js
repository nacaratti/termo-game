import { createGameState } from './gameStateFactory';

const {
  saveGameProgress: saveGameProgress6,
  saveCompletedGame: saveCompletedGame6,
  getSavedGame: getSavedGame6,
} = createGameState('_p3q6');

export { saveGameProgress6, saveCompletedGame6, getSavedGame6 };
